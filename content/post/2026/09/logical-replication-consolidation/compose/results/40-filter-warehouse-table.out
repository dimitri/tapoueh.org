-- @service: warehouse
-- Step 2a: the warehouse copy of shop.customers is created WITHOUT the PII columns
-- (email, phone). The subscriber table only needs the columns that will be sent.
create table shop.customers (id int primary key, account_id int not null, name text not null, country text, tenant text not null);
CREATE TABLE
alter table shop.customers owner to sub_shop;
ALTER TABLE
