#author Dimitri Fontaine
#title /dev/dim/projects/cssh.el
#date 20081206
#desc Tapoueh.org Inc. Projects

* ClusterSSH for Emacs

<include file="prj.cssh.muse">

** =cssh.el= going crazy

What happens when you try <code>C-M-= . RET</code>? =cssh.el= will
happily open all matching entries found in =~/.ssh/known_hosts=, and that
could be a lot.

			[[http://p.hagelb.org/cssh-crazy.png][../images/cssh-crazy.thumb.png]]

